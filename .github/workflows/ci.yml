name: ci

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  swift:
    runs-on: macos-latest
    steps:
    - uses: actions/checkout@v4

    - name: Test
      uses: mxcl/xcodebuild@v2

    - name: Lint
      run: swiftlint
